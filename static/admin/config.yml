backend:
  name: git-gateway
  branch: main
  commit_messages:
    create: 'Create {{collection}} "{{slug}}"'
    update: 'Update {{collection}} "{{slug}}"'
    delete: 'Delete {{collection}} "{{slug}}"'
    uploadMedia: '[skip ci] Upload "{{path}}"'
    deleteMedia: '[skip ci] Delete "{{path}}"'

local_backend: true
media_folder: static/images/uploads
public_folder: /images/uploads

# Remove Decap CMS branding
site_url: https://wrong.athrvakhrbde.com
display_url: https://wrong.athrvakhrbde.com
logo_url: /images/logo.png

collections:
  - name: "posts"
    label: "Posts"
    folder: "content/post"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "hero", widget: "image", required: false}
      - {label: "Excerpt", name: "excerpt", widget: "text", required: true}
      - label: "Authors"
        name: "authors"
        widget: "list"
        default: ["Wrong Team"]
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "authors"
    label: "Authors"
    folder: "content/authors"
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Name", name: "title", widget: "string"}
      - {label: "Bio", name: "bio", widget: "text"}
      - {label: "Featured", name: "featured", widget: "boolean", default: false}
      - label: "Social"
        name: "social"
        widget: "list"
        fields:
          - {label: "Title", name: "title", widget: "string"}
          - {label: "URL", name: "url", widget: "string"}

  - name: "pages"
    label: "Pages"
    files:
      - file: "content/_index.md"
        label: "Home"
        name: "home"
        fields:
          - {label: "Title", name: "title", widget: "string", default: "Atharva might be wrong, but..."}
          - label: "Hero"
            name: "hero"
            widget: "object"
            fields:
              - {label: "Heading", name: "heading", widget: "string", default: "Atharva might be wrong, but..."}
              - {label: "Max Width", name: "maxWidth", widget: "hidden", default: 652}
          - {label: "Body", name: "body", widget: "markdown"} 